# GitHub Setup Commands

After creating the repository on GitHub, run these commands:

```powershell
# Add GitHub as remote (replace YOUR_USERNAME with your GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/rcoscan-scanner.git

# Rename branch to main
git branch -M main

# Push to GitHub
git push -u origin main
```

Your repository will be at: https://github.com/YOUR_USERNAME/rcoscan-scanner

## QR Code for Expo Go

Users can access your app via Expo Go by:
1. Opening the Expo Go app
2. Searching for: `@artichoke/rcoscan-scanner`
3. Or visiting: https://expo.dev/@artichoke/rcoscan-scanner

To share directly, you can generate a QR code at:
https://expo.dev/@artichoke/rcoscan-scanner
